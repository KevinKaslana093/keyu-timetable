# 课屿 1.1.1 读取修复体验版

修复网页课表读取的结构限制：支持多层同源内嵌页面、课程单元格中的嵌套表格、带附加日期的星期标题，并为分离的表头/课程区域增加列位置识别。

识别失败会说明原因，提供“复制诊断”，诊断只含表格/框架/星期标题数量和版本，不含密码、Cookie、姓名或课程内容。

由用户截图定位到旧读取器的局限；尚未取得该账号页面的真实 HTML，不能据截图声称已在用户真实教务数据上复现并解决。已使用相应结构的虚构页面回归验证，真实页面请更新后再读取；如失败，复制诊断反馈。

沿用原包名与签名，可覆盖更新，已有课表保留。保留 PDF 导入及五山、大学城 / 国际校区作息。
