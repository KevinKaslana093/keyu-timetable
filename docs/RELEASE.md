# 课屿 1.1.4 提取兼容体验版

针对页面已经检测到星期标题和课程块，但 extract 抛出 TypeError 的反馈，移除学校页面可能覆盖的 Map/Set 依赖，使用数组保存列与去重记录。改用基础 DOM 操作提取文字，不依赖 closest/remove/replaceWith/prepend/append。保留星期、周次和课程预览校验。

诊断进一步标注表头、表格定位、课程文字或列位置步骤，不复制异常原文。已用虚构页面覆盖 Map 的方式复现 1.1.3 的 extract TypeError，同一页面在新版本读取成功；真实设备的具体触发点尚未直接验证。

可覆盖安装保留课表。若仍失败，复制新版诊断；PDF 导入继续可用。
